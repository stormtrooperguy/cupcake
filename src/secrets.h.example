#pragma once

// Copy this file to secrets.h and set your own password (8-63 chars).
#define AP_PASSWORD "changeme"

// Password for the shared fazbear_sec network (hosted by springtrap).
// Must match that device's AP_PASSWORD or the join attempt will fail --
// falls back to running on the Cupcake AP alone either way.
#define FAZBEAR_PASSWORD "changeme"
